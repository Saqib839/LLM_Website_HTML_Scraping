beautifulsoup4>=4.12.2
requests>=2.28
transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.1.98
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Note: Installing torch may pick a heavy download. 
# Llama-3.2-3B requires at least 6GB of VRAM or will use CPU (slower)
# For optimal performance, use GPU with at least 8GB VRAM